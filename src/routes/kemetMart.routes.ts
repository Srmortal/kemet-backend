// import { Router } from 'express';
// import { getProducts, getCart, checkout, addToCart, removeFromCart, updateItemQuantity } from '@controllers/kemetMart.controller';
// import { firebaseAuth } from '@middleware/firebaseAuth';
// // import { validateRequest } from '@middleware/validateRequest';
// // import { addToCartParamsSchema, checkoutSchema, updateItemQuantitySchema } from '@validators/kemetMart.joi';

// const router = Router();

// // All kemet-mart routes require authentication
// router.use(firebaseAuth);

// router.get('/products', getProducts);
// router.get('/cart', getCart);
// router.post('/cart', addToCart);
// router.patch('/cart/:id', updateItemQuantity);
// router.delete('/cart/:id', removeFromCart);
// router.post('/checkout', checkout);

// export default router;
// // router.post('/checkout', validateRequest(checkoutSchema), checkout);
