// import { Router } from 'express';
// import { createExchangeBooking } from '../controllers/currencyExchange.controller';
// import { firebaseAuth } from '@middleware/firebaseAuth';
// // import { validateRequest } from '@middleware/validateRequest';
// // import { createExchangeBookingSchema } from '@validators/currencyExchange.validator';

// const router = Router();
// router.use(firebaseAuth);

// router.post('/book', createExchangeBooking);

// export default router;
